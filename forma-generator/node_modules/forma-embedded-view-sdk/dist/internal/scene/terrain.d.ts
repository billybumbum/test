import { IframeMessenger } from "../iframe-messenger.js";
type Position = {
    x: number;
    y: number;
    z: number;
};
type Scale = {
    x: number;
    y: number;
};
export type GroundTextureAddRequest = {
    name: string;
    canvas: HTMLCanvasElement;
    position: Position;
    scale?: Scale | undefined;
};
export type GroundTextureUpdateTextureDataRequest = {
    name: string;
    canvas: HTMLCanvasElement;
};
export type GroundTextureUpdatePositionRequest = {
    name: string;
    position: Position;
};
export type GroundTextureRemoveRequest = {
    name: string;
};
export declare class TerrainApi {
    groundTexture: GroundTextureApi;
    constructor(iframeMessenger: IframeMessenger);
}
declare class GroundTextureApi {
    #private;
    constructor(iframeMessenger: IframeMessenger);
    add({ name, canvas, position, scale }: GroundTextureAddRequest): Promise<void>;
    updateTextureData({ name, canvas, }: GroundTextureUpdateTextureDataRequest): Promise<void>;
    updatePosition({ name, position, }: GroundTextureUpdatePositionRequest): Promise<void>;
    remove({ name }: GroundTextureRemoveRequest): Promise<void>;
}
export {};
