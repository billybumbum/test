import { IframeMessenger } from "./iframe-messenger.js";
export type AddElementRequest = {
    /**
     * The urn of the element to add.
     */
    urn: string;
    /**
     * The path where in the tree to add the element.
     *
     * Only supported are proposal (root) and base (root/<base key>).
     *
     * @defaultValue root
     */
    parentPath?: string;
    /**
     * The transformation matrix to position the element
     * relative to the parent.
     *
     * If the default value of the idientity matrix is used,
     * then the position is relative to the origin of the parent.
     * When using the proposal as parent then the origin is the
     * reference point of the proposal.
     *
     * @defaultValue identity matrix
     */
    transform?: number[];
    /**
     * The name of the element.
     *
     * @defaultValue undefined
     */
    name?: string;
};
export declare class ProposalApi {
    #private;
    constructor(iframeMessenger: IframeMessenger);
    getRootUrn(): Promise<string>;
    getId(): Promise<string>;
    addElement(request: AddElementRequest): Promise<void>;
}
